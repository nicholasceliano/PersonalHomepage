import { OAuthVideoPlayerPanel } from './oauth-video-player-panel';

describe('OAuthVideoPlayerPanel', () => {
	it('should create an instance', () => {
		expect(new OAuthVideoPlayerPanel()).toBeTruthy();
	});
});
