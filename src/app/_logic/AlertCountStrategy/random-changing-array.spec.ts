import { RandomChangingArrayAlertCountStrategy } from './random-changing-array';

describe('RandomChangingArray', () => {
	it('should create an instance', () => {
		expect(new RandomChangingArrayAlertCountStrategy('')).toBeTruthy();
	});
});
