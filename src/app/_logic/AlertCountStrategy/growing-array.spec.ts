import { GrowingArrayAlertCountStrategy } from './growing-array';

describe('GrowingArray', () => {
	it('should create an instance', () => {
		expect(new GrowingArrayAlertCountStrategy()).toBeTruthy();
	});
});
