import { FullOrderedArrayAlertCountStrategy } from './full-ordered-array';

describe('FullOrderedArray', () => {
	it('should create an instance', () => {
		expect(new FullOrderedArrayAlertCountStrategy()).toBeTruthy();
	});
});
