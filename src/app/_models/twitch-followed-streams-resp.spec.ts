import { TwitchFollowedStreamsResp } from './twitch-followed-streams-resp';

describe('TwitchFollowedStreamsResp', () => {
  it('should create an instance', () => {
    expect(new TwitchFollowedStreamsResp()).toBeTruthy();
  });
});
